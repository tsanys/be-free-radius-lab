# Database administrative login by Unix domain socket
local   all             postgres                                peer

# Allow FreeRADIUS DB user 'rapid' to connect from localhost using password
host    radius          rapid           127.0.0.1/32            md5
host    radius          rapid           ::1/128                 md5

# Allow any user to connect to any database from any IP using password
host    all             all             0.0.0.0/0               md5
host    all             all             ::/0                    md5

# "local" is for Unix domain socket connections only
local   all             all                                     peer

# Allow replication connections from localhost
local   replication     all                                     peer
host    replication     all             127.0.0.1/32            md5
host    replication     all             ::1/128                 md5
